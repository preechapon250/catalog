# syntax=dhi.io/build:2-helm3

name: OpenSearch Dashboards Helm chart 3.x
image: dhi.io/opensearch-dashboards-chart
variant: helm
tags:
  - 3.4.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: faa0693a5f5ff85b2804bc5934a6bea19709ef6d
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.19.5-debian13-dev@sha256:9fa0f8ba70052f17df5a7a3469c2e306d18b5f934f3439ce781c9dd5a2f6b96a
  OPENSEARCH_DASHBOARDS_REFERENCE: dhi/opensearch-dashboards:3.4.0-debian13@sha256:0d8e585a8d40fc729ce25e522ffb1e994968d6414fb8e418540c76b147c286a0
  SEMVER_MAJOR_MINOR_VERSION: "3.4"
  SEMVER_MAJOR_VERSION: "3"
  SEMVER_VERSION: 3.4.0
  VERSION: 3.4.0
contents:
  builds:
    - name: opensearch-dashboards
      uses: dhi.io/helm:3.19.5-debian13-dev@sha256:9fa0f8ba70052f17df5a7a3469c2e306d18b5f934f3439ce781c9dd5a2f6b96a
      contents:
        files:
          - url: git+https://github.com/opensearch-project/helm-charts.git#opensearch-dashboards-3.4.0
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/opensearch-dashboards
          with:
            chart:
              appRef: opensearch-dashboards
              description: Docker Hardened Images Helm chart for OpenSearch Dashboards
              images:
                - name: opensearch-dashboards
                  ref: dhi/opensearch-dashboards:3.4.0-debian13@sha256:0d8e585a8d40fc729ce25e522ffb1e994968d6414fb8e418540c76b147c286a0
              name: opensearch-dashboards-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/opensearch-project/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/opensearch-dashboards
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/charts/opensearch-dashboards
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: opensearch-dashboards
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/charts/opensearch-dashboards
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/opensearch-dashboards/config
