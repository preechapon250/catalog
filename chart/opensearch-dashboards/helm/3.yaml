# syntax=dhi.io/build:2-helm3

name: OpenSearch Dashboards Helm chart 3.x
image: dhi.io/opensearch-dashboards-chart
variant: helm
tags:
  - 3.5.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 30f89f9dd44508217eea818fb52638e3d4eb1524
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.20.0-debian13-dev@sha256:7069456b86d57f9f2efaf7561c7f78097c6a4382a6e3420d23680ce61a045f34
  OPENSEARCH_DASHBOARDS_REFERENCE: dhi/opensearch-dashboards:3.5.0-debian13@sha256:83810e3629e42903ceeece976118704ad360420ced7b9eb4bfc54f844e78a4fb
  SEMVER_MAJOR_MINOR_VERSION: "3.5"
  SEMVER_MAJOR_VERSION: "3"
  SEMVER_VERSION: 3.5.0
  VERSION: 3.5.0
contents:
  builds:
    - name: opensearch-dashboards
      uses: dhi.io/helm:3.20.0-debian13-dev@sha256:7069456b86d57f9f2efaf7561c7f78097c6a4382a6e3420d23680ce61a045f34
      contents:
        files:
          - url: git+https://github.com/opensearch-project/helm-charts.git#opensearch-dashboards-3.5.0
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/opensearch-dashboards
          with:
            chart:
              appRef: opensearch-dashboards
              description: Docker Hardened Images Helm chart for OpenSearch Dashboards
              images:
                - name: opensearch-dashboards
                  ref: dhi/opensearch-dashboards:3.5.0-debian13@sha256:83810e3629e42903ceeece976118704ad360420ced7b9eb4bfc54f844e78a4fb
              name: opensearch-dashboards-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/opensearch-project/helm-charts
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/opensearch-dashboards
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/charts/opensearch-dashboards
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: opensearch-dashboards
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/charts/opensearch-dashboards
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/opensearch-dashboards/config
