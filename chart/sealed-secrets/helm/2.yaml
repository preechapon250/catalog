# syntax=dhi/build:1-helm3

# DO NOT EDIT THIS FILE MANUALLY
# generated with 'dhi def merge chart/sealed-secrets/helm/2.yaml'

name: Sealed Secrets Helm chart 0.x
image: dhi/sealed-secrets-chart
variant: helm
tags:
    - 0.34.0
platforms:
    - linux/amd64
vars:
    CHART_COMMIT_SHA: fd30a3eb590170d99e5d62df254d097a2c131144
    DHI_NAMESPACE: dhi
    DHI_PREFIX: ""
    DHI_REGISTRY: dhi.io
    DHI_SKIP_CONTEXT_COPY: "true"
    HELM_REFERENCE: dhi/helm:3.19.4-debian13-dev@sha256:705dc44463a159c5163cc790e19afd3ca2071d63a5c3c7951e4173e1cbb14f0a
    SEALED_SECRETS_REFERENCE: dhi/sealed-secrets-controller:0.34.0-debian13@sha256:0c7586f4b4ba354c0d1f40af0f7ca011ab5062cb71edd41ef6007b90e48b67cc
    SEMVER_MAJOR_MINOR_VERSION: "0.34"
    SEMVER_MAJOR_VERSION: "0"
    SEMVER_VERSION: 0.34.0
    VERSION: 0.34.0
contents:
    builds:
        - name: sealed-secrets-chart
          uses: dhi/helm:3.19.4-debian13-dev@sha256:705dc44463a159c5163cc790e19afd3ca2071d63a5c3c7951e4173e1cbb14f0a
          contents:
            files:
                - url: git+https://github.com/bitnami-labs/sealed-secrets.git#v0.34.0
                  path: ${source.dir}/helm-charts
                  uid: 0
                  gid: 0
          pipeline:
            - name: prepare chart
              uses: helm/rudder@v1
              work-dir: ${source.dir}/helm-charts/helm/sealed-secrets
              with:
                chart:
                    appRef: sealed-secrets-controller
                    description: Docker Hardened Images Helm chart for Sealed Secrets
                    images:
                        - name: sealed-secrets-controller
                          ref: dhi/sealed-secrets-controller:0.34.0-debian13@sha256:0c7586f4b4ba354c0d1f40af0f7ca011ab5062cb71edd41ef6007b90e48b67cc
                    name: sealed-secrets-chart
                    notes: ${source.dir}/config/NOTES.txt
                    sourceUrl: https://github.com/bitnami-labs/sealed-secrets
                cmd: prepare
                registry: dhi.io
            - name: relocate chart
              uses: helm/rudder@v1
              work-dir: ${source.dir}/helm-charts/helm/sealed-secrets
              with:
                cmd: patch
                patches:
                    from: ${source.dir}/config
                registry: dhi.io
            - name: update dependencies
              uses: helm/cmd@v1
              secrets:
                - id: docker_hub_user
                  env: DOCKER_HUB_USER
                - id: docker_hub_password
                  env: DOCKER_HUB_PASSWORD
              with:
                chart: ${source.dir}/helm-charts/helm/sealed-secrets
                cmd: dependency update
            - name: lint
              uses: helm/cmd@v1
              work-dir: ${source.dir}/helm-charts/helm
              with:
                chart: sealed-secrets
                cmd: lint .
            - name: package
              uses: helm/package@v1
              with:
                chart: ${source.dir}/helm-charts/helm/sealed-secrets
          paths:
            - type: directory
              path: ${source.dir}/config
              uid: 0
              gid: 0
              mode: "0644"
              source: chart/sealed-secrets/config
tests:
    - name: Run sealed-secrets testcontainers helm tests
      directory: chart/sealed-secrets/tests
      commands:
        - go test . -count=1 -v
      exit-code: 0
    - name: Run base helm tests
      directory: test/go-testing/k8s/helm/base
      commands:
        - go test . -count=1 -v
      exit-code: 0
