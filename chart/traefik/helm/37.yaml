# syntax=dhi/build:1-helm3

# DO NOT EDIT THIS FILE MANUALLY
# generated with 'dhi def merge chart/traefik/helm/37.yaml'

name: Traefik Helm chart 37.x
image: dhi/traefik-chart
variant: helm
tags:
    - 37.4.0
platforms:
    - linux/amd64
vars:
    CHART_COMMIT_SHA: 75bdb79d11e23560eebb0302f030121e5cfb081f
    COMMIT_SHA: 64d25f2c7dd932b3b7dc27d0bfa6ea131fc280f3
    DHI_NAMESPACE: dhi
    DHI_PREFIX: ""
    DHI_REGISTRY: dhi.io
    DHI_SKIP_CONTEXT_COPY: "true"
    HELM_REFERENCE: dhi/helm:3.19.4-debian13-dev@sha256:705dc44463a159c5163cc790e19afd3ca2071d63a5c3c7951e4173e1cbb14f0a
    SEMVER_MAJOR_MINOR_VERSION: "37.4"
    SEMVER_MAJOR_VERSION: "37"
    SEMVER_VERSION: 37.4.0
    TRAEFIK_REFERENCE: dhi/traefik:3.6.4-debian13@sha256:758e2156f9e5394c4690e186945323046a99bfe052da3d5f90f96f715d85b6a7
    TRAEFIK_VERSION: 3.6.5
    VERSION: 37.4.0
contents:
    builds:
        - name: traefik
          uses: dhi/helm:3.19.4-debian13-dev@sha256:705dc44463a159c5163cc790e19afd3ca2071d63a5c3c7951e4173e1cbb14f0a
          contents:
            files:
                - url: git+https://github.com/traefik/traefik-helm-chart.git#v37.4.0
                  path: ${source.dir}/helm-charts
                  uid: 0
                  gid: 0
          pipeline:
            - name: prepare chart
              uses: helm/rudder@v1
              work-dir: ${source.dir}/helm-charts/traefik
              with:
                chart:
                    appRef: traefik
                    description: Docker Hardened Images Helm chart for Traefik
                    images:
                        - name: traefik
                          ref: dhi/traefik:3.6.4-debian13@sha256:758e2156f9e5394c4690e186945323046a99bfe052da3d5f90f96f715d85b6a7
                    name: traefik-chart
                    notes: ${source.dir}/config/NOTES.txt
                    sourceUrl: https://github.com/traefik/traefik-helm-chart
                cmd: prepare
                registry: dhi.io
            - name: relocate chart
              uses: helm/rudder@v1
              work-dir: ${source.dir}/helm-charts/traefik
              with:
                cmd: patch
                patches:
                    from: ${source.dir}/config
                registry: dhi.io
            - name: update dependencies
              uses: helm/cmd@v1
              with:
                chart: ${source.dir}/helm-charts/traefik
                cmd: dependency update
            - name: lint
              uses: helm/cmd@v1
              work-dir: ${source.dir}/helm-charts
              with:
                chart: traefik
                cmd: lint .
            - name: package
              uses: helm/package@v1
              with:
                chart: ${source.dir}/helm-charts/traefik
          paths:
            - type: directory
              path: ${source.dir}/config
              uid: 0
              gid: 0
              mode: "0644"
              source: chart/traefik/config
tests:
    - name: Run traefik testcontainers helm tests
      directory: chart/traefik/tests
      commands:
        - go test . -count=1 -v
      exit-code: 0
    - name: Run base helm tests
      directory: test/go-testing/k8s/helm/base
      commands:
        - go test . -count=1 -v
      exit-code: 0
