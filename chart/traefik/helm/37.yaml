# syntax=dhi.io/build:2-helm3

name: Traefik Helm chart 37.x
image: dhi.io/traefik-chart
variant: helm
tags:
  - 37.4.0
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 75bdb79d11e23560eebb0302f030121e5cfb081f
  COMMIT_SHA: 64d25f2c7dd932b3b7dc27d0bfa6ea131fc280f3
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.20.0-debian13-dev@sha256:a9247b60e56f190fb6de43042004c80b6ad903fe8df5becc513d89066aa3221c
  SEMVER_MAJOR_MINOR_VERSION: "37.4"
  SEMVER_MAJOR_VERSION: "37"
  SEMVER_VERSION: 37.4.0
  TRAEFIK_REFERENCE: dhi/traefik:3.6.7-debian13@sha256:8bbd10803533d3c7385bd50bf994b1ca2ddc3c4fd25ce20a77e0f6446b4bc955
  TRAEFIK_VERSION: 3.6.5
  VERSION: 37.4.0
contents:
  builds:
    - name: traefik
      uses: dhi.io/helm:3.20.0-debian13-dev@sha256:a9247b60e56f190fb6de43042004c80b6ad903fe8df5becc513d89066aa3221c
      contents:
        files:
          - url: git+https://github.com/traefik/traefik-helm-chart.git#v37.4.0
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            chart:
              appRef: traefik
              description: Docker Hardened Images Helm chart for Traefik
              images:
                - name: traefik
                  ref: dhi/traefik:3.6.7-debian13@sha256:8bbd10803533d3c7385bd50bf994b1ca2ddc3c4fd25ce20a77e0f6446b4bc955
              name: traefik-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/traefik/traefik-helm-chart
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/traefik
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/traefik
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts
          with:
            chart: traefik
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/traefik
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/traefik/config
