Subject: [PATCH] use direct path to runsvdir instead of which lookup
# Source: inline sed commands from upstream Dockerfile
# Replace the which-based runsvdir lookup with a direct path to /usr/local/bin/runsvdir,
# since runsvdir is installed from source and may not be in the default PATH.
---
diff --git a/node/filesystem/sbin/start_runit b/node/filesystem/sbin/start_runit
--- a/node/filesystem/sbin/start_runit
+++ b/node/filesystem/sbin/start_runit
@@ -14,4 +14,3 @@ fi
 export NODENAME=$(cat /var/lib/calico/nodename)

-RUNSVDIR=$(/usr/bin/which runsvdir)
-exec ${RUNSVDIR} -P /etc/service/enabled
+exec /usr/local/bin/runsvdir -P /etc/service/enabled
