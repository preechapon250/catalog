# syntax=dhi.io/build:2-debian13

name: Istio Ztunnel 1.27.x
image: dhi.io/ztunnel
variant: runtime
tags:
  - "1.27"
  - 1.27.5
  - 1.27-debian13
  - 1.27.5-debian13
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2025-08-11"
  end-of-life: "2026-04-30"
vars:
  BASE_DIGEST: sha256:74fc43fa240887b8159970e434244039aab0c6efaaa9cf044004cdc22aa2a34d
  BASE_TAG: 20250419-debian13
  COMMIT_SHA: 055baef94d1d1ec90859196ce015ac0bba587503
  RUST_REFERENCE: dhi/rust:1.92.0-debian13-dev@sha256:c428273859121872a05d4f28176d59ea38bd59d08e59f4d25f4ed3658e9b6720
  RUST_VERSION: 1.92.0
  SEMVER_MAJOR_MINOR_VERSION: "1.27"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.27.5
  VERSION: 1.27.5
contents:
  packages:
    - '!libelogind0'
    - '!mawk'
    - '!original-awk'
    - base-files
  builds:
    - name: ztunnel
      uses: dhi.io/rust:1.92.0-debian13-dev@sha256:c428273859121872a05d4f28176d59ea38bd59d08e59f4d25f4ed3658e9b6720
      environment:
        VERSION: 1.27.5
      contents:
        packages:
          - bash
          - sed
          - protobuf-compiler
          - pkg-config
          - libssl-dev
        files:
          - url: git+https://github.com/istio/ztunnel.git#1.27.5
            checksum: 055baef94d1d1ec90859196ce015ac0bba587503
            path: ${source.dir}/ztunnel-055baef94d1d1ec90859196ce015ac0bba587503
            spdx:
              name: ztunnel
              version: 1.27.5
              packages:
                - name: ztunnel
                  purl: pkg:dhi/ztunnel@1.27.5
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: patch CVEs
          work-dir: ${source.dir}/ztunnel-055baef94d1d1ec90859196ce015ac0bba587503
          privileged: true
          runs: |
            set -eux -o pipefail

            # Removed test dependencies
            rm -rf ./fuzz/Cargo.*

            # Fixes CVE-2024-7254 & CVE-2025-53605
            sed -i \
              's/pprof = { version = "0\.14"/pprof = { version = "0\.15"/g' \
              Cargo.toml

            # Fixes CVE-2025-58160 (ANSI escape sequence injection in tracing-subscriber)
            sed -i \
              's/tracing-subscriber = { version = "0\.3[^"]*"/tracing-subscriber = { version = ">=0.3.20, <0.4"/g' \
              Cargo.toml

            # Update all patched dependencies
            cargo update --ignore-rust-version --recursive pprof tracing-subscriber
        - name: cargo sbom
          uses: sbom@v1
          with:
            extra-scanners: rust-cargo-lock-cataloger
            prefix: ztunnel-cargo
            source: ${source.dir}/ztunnel-055baef94d1d1ec90859196ce015ac0bba587503
        - name: build
          work-dir: ${source.dir}/ztunnel-055baef94d1d1ec90859196ce015ac0bba587503
          privileged: true
          runs: |
            set -eux -o pipefail

            CARGO_FLAGS=$([ "${target.fips}" = "true" ] && echo "--no-default-features --features tls-openssl" || echo "")

            cargo build --release --locked --target-dir "${target.dir}" $CARGO_FLAGS
            install -D -m 755 "${target.dir}/release/ztunnel" "${target.dir}/usr/local/bin/ztunnel"
      outputs:
        - source: ${target.dir}/usr/local/bin/ztunnel
          target: /usr/local/bin/ztunnel
          uid: 0
          gid: 0
        - source: /opt/docker/sbom/ztunnel
          target: /opt/docker/sbom/ztunnel
          uid: 0
          gid: 0
        - source: /opt/docker/sbom/ztunnel-cargo
          target: /opt/docker/sbom/ztunnel-cargo
          uid: 0
          gid: 0
  artifacts:
    - name: dhi.io/static:20250419-debian13@sha256:74fc43fa240887b8159970e434244039aab0c6efaaa9cf044004cdc22aa2a34d
      includes:
        - bin
        - etc
        - home
        - lib
        - lib64
        - opt
        - sbin
        - tmp
        - usr
        - var
      excludes:
        - etc/group
        - etc/os-release
        - etc/passwd
        - var/lib/dpkg/status
      uid: 0
      gid: 0
accounts:
  run-as: nonroot
  users:
    - name: nonroot
      uid: 65532
      gid: 65532
  groups:
    - name: nonroot
      gid: 65532
      members:
        - nonroot
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
annotations:
  org.opencontainers.image.description: The ztunnel component of Istio's ambient mesh
  org.opencontainers.image.licenses: Apache-2.0
entrypoint:
  - ztunnel
