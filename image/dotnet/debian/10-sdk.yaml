# syntax=dhi.io/build:2-debian13

name: .NET 10.x SDK
image: dhi.io/dotnet
variant: runtime
tags:
  - 10-sdk
  - 10.0-sdk
  - 10.0.101-sdk
  - 10-sdk-debian13
  - 10.0-sdk-debian13
  - 10.0.101-sdk-debian13
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2025-11-11"
  end-of-life: "2028-11-14"
vars:
  ARCH: '#{ target.arch == "amd64" ? "x64" : "arm64" }'
  CREDPROVIDER_VERSION: 1.4.1
  SEMVER_MAJOR_MINOR_VERSION: "10.0"
  SEMVER_MAJOR_VERSION: "10"
  SEMVER_VERSION: 10.0.101
  VERSION: 10.0.101
contents:
  packages:
    - '!gawk'
    - '!libelogind0'
    - '!mawk'
    - '!original-awk'
    - apt
    - base-files
    - bash
    - ca-certificates
    - coreutils
    - diffutils
    - dpkg
    - findutils
    - grep
    - libc-bin
    - libc6
    - libgcc-s1
    - libgssapi-krb5-2
    - libicu76
    - libssl3t64
    - libstdc++6
    - mawk
    - perl-base
    - sed
    - tzdata
    - util-linux
    - zlib1g
  files:
    - url: https://builds.dotnet.microsoft.com/dotnet/Sdk/10.0.101/dotnet-sdk-10.0.101-linux-${ARCH}.tar.gz
      path: /opt/dotnet
      spdx:
        name: dotnet-sdk
        version: 10.0.101
        packages:
          - name: dotnet-sdk
            purl: pkg:dhi/microsoft/dotnet-sdk@10.0.101
            license: MIT
      uid: 0
      gid: 0
  builds:
    - name: azurecreds
      contents:
        packages:
          - bash
          - coreutils
          - gzip
          - tar
        files:
          - url: https://packages.microsoft.com/keys/microsoft-2025.asc
            path: ${source.dir}/microsoft.asc
            uid: 0
            gid: 0
          - url: https://github.com/microsoft/artifacts-credprovider/releases/download/v1.4.1/Microsoft.Net8.linux-${ARCH}.NuGet.CredentialProvider.tar.gz.asc
            path: ${source.dir}/credprovider.tar.gz.asc
            uid: 0
            gid: 0
          - url: https://github.com/microsoft/artifacts-credprovider/releases/download/v1.4.1/Microsoft.Net8.linux-${ARCH}.NuGet.CredentialProvider.tar.gz
            path: ${source.dir}/credprovider.tar.gz
            uid: 0
            gid: 0
      pipeline:
        - name: verify
          uses: gpg/verify@v1
          with:
            file: ${source.dir}/credprovider.tar.gz
            keys: ${source.dir}/microsoft.asc
            signature: ${source.dir}/credprovider.tar.gz.asc
        - name: install
          runs: |
            set -eux -o pipefail

            mkdir -p "${target.dir}/root/.nuget"
            tar -xzf "${source.dir}/credprovider.tar.gz" -C "${target.dir}/root/.nuget" --strip-components=1
      outputs:
        - source: ${target.dir}/root/.nuget
          target: /root/.nuget
          uid: 0
          gid: 0
accounts:
  root: true
  run-as: root
  users:
    - name: nonroot
      uid: 65532
      gid: 65532
    - name: _apt
      uid: 42
      gid: 65532
  groups:
    - name: nonroot
      gid: 65532
      members:
        - nonroot
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
work-dir: /app
environment:
  DEBIAN_FRONTEND: noninteractive
  DOTNET_CLI_TELEMETRY_OPTOUT: "true"
  DOTNET_GENERATE_ASPNET_CERTIFICATE: "false"
  DOTNET_ROOT: /opt/dotnet
  DOTNET_RUNNING_IN_CONTAINER: "true"
  DOTNET_VERSION: 10.0.101
  PATH: /opt/dotnet:/opt/dotnet/tools:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
  SSL_CERT_FILE: /etc/ssl/certs/ca-certificates.crt
paths:
  - type: directory
    path: /tmp
    uid: 0
    gid: 0
    mode: "0777"
annotations:
  org.opencontainers.image.description: A minimal .NET SDK image
  org.opencontainers.image.licenses: MIT
cmd:
  - dotnet
  - --help
