# syntax=dhi.io/build:2-debian13

name: Elasticsearch 9.2.x
image: dhi.io/elasticsearch
variant: runtime
tags:
  - "9"
  - "9.2"
  - 9.2.4
  - 9-debian13
  - 9.2-debian13
  - 9.2.4-debian13
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2025-10-21"
vars:
  ARCH: '#{ target.arch == "amd64" ? "amd64" : "arm64" }'
  ELASTICSEARCH_COMMIT_SHA: dfc5c38614c29a598e132c035b66160d3d350894
  ELASTICSEARCH_OUTPUT_ARCHIVE_DIR: '#{ target.arch == "amd64" ? "linux-tar" : "linux-aarch64-tar" }'
  GO_ARCH: '#{ target.arch == "amd64" ? "x86_64" : "aarch64" }'
  JDK_DIGEST: sha256:38b228ea69b2f3cbd3202741e73bb3115217a6283be4955e8f9e181d9a3156df
  JDK_VERSION: 21.0.9.10
  SEMVER_MAJOR_MINOR_VERSION: "9.2"
  SEMVER_MAJOR_VERSION: "9"
  SEMVER_VERSION: 9.2.4
  SKIP_PATCHING: "FALSE"
  VERSION: 9.2.4
contents:
  packages:
    - '!libelogind0'
    - '!mawk'
    - '!original-awk'
    - base-files
    - bash
    - coreutils
    - findutils
    - grep
    - libstdc++6
    - tini
  builds:
    - name: elasticsearch
      uses: dhi.io/eclipse-temurin:21.0.9.10-jdk-debian13-dev@sha256:38b228ea69b2f3cbd3202741e73bb3115217a6283be4955e8f9e181d9a3156df
      privileged: true
      contents:
        packages:
          - bash
          - coreutils
          - findutils
          - sed
          - tini
        files:
          - url: git+https://github.com/elastic/elasticsearch.git#v9.2.4
            checksum: dfc5c38614c29a598e132c035b66160d3d350894
            path: ${source.dir}/elasticsearch
            spdx:
              name: elasticsearch
              version: 9.2.4
              packages:
                - name: elasticsearch
                  purl: pkg:dhi/elasticsearch@9.2.4
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: git patch elasticsearch
          uses: patch@v1
          work-dir: ${source.dir}/elasticsearch
          with:
            options: -p1
            patches:
              - ${source.dir}/patch/9.2.patch
        - name: build
          work-dir: ${source.dir}/elasticsearch
          runs: |
            set -eux -o pipefail

            ./gradlew localDistro
        - name: extract built tarball
          runs: |
            set -eux -o pipefail

            mkdir -p "${target.dir}/opt/elasticsearch/"
            cp -r "${source.dir}/elasticsearch/distribution/archives/${ELASTICSEARCH_OUTPUT_ARCHIVE_DIR}/build/install/elasticsearch-9.2.4-SNAPSHOT/." "${target.dir}/opt/elasticsearch/elasticsearch-9.2.4"
      paths:
        - type: file
          path: ${source.dir}/patch/9.2.patch
          uid: 0
          gid: 0
          source: image/elasticsearch/patch/9.2.patch
      outputs:
        - source: ${target.dir}/opt/elasticsearch/elasticsearch-9.2.4
          target: /opt/elasticsearch/elasticsearch-9.2.4
          uid: 1000
          gid: 1000
        - source: /opt/docker/sbom/elasticsearch
          target: /opt/docker/sbom/elasticsearch
          uid: 0
          gid: 0
        - source: ${source.dir}/elasticsearch/distribution/docker/src/docker/bin/docker-entrypoint.sh
          target: /usr/local/bin/docker-entrypoint.sh
          uid: 1000
          gid: 1000
          mode: "0755"
accounts:
  run-as: elasticsearch
  users:
    - name: elasticsearch
      uid: 1000
      gid: 1000
  groups:
    - name: elasticsearch
      gid: 1000
      members:
        - elasticsearch
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
work-dir: /usr/share/elasticsearch
environment:
  ELASTIC_CONTAINER: "true"
  ELASTICSEARCH_VERSION: 9.2.4
  PATH: /opt/elasticsearch/elasticsearch-9.2.4/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
paths:
  - type: symlink
    path: /usr/share/elasticsearch
    uid: 0
    gid: 0
    source: /opt/elasticsearch/elasticsearch-9.2.4
  - type: file
    path: /opt/elasticsearch/elasticsearch-9.2.4/config/elasticsearch.yml
    uid: 1000
    gid: 1000
    mode: "0644"
    source: image/elasticsearch/config/elasticsearch.yml
  - type: file
    path: /opt/elasticsearch/elasticsearch-9.2.4/config/log4j2.properties
    uid: 1000
    gid: 1000
    mode: "0644"
    source: image/elasticsearch/config/log4j2.properties
annotations:
  org.opencontainers.image.description: A minimal Elasticsearch image
  org.opencontainers.image.licenses: Elastic-License-2.0
entrypoint:
  - /usr/bin/tini
  - --
  - /usr/local/bin/docker-entrypoint.sh
cmd:
  - eswrapper
ports:
  - 9200/tcp
  - 9300/tcp
