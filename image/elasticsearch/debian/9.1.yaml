# syntax=dhi.io/build:2-debian13

name: Elasticsearch 9.1.x
image: dhi.io/elasticsearch
variant: runtime
tags:
  - "9.1"
  - 9.1.9
  - 9.1-debian13
  - 9.1.9-debian13
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2025-07-23"
vars:
  ARCH: '#{ target.arch == "amd64" ? "amd64" : "arm64" }'
  ELASTICSEARCH_COMMIT_SHA: 5cf7cb8c6f69b8c284f3ea738db6932703574d3d
  ELASTICSEARCH_OUTPUT_ARCHIVE_DIR: '#{ target.arch == "amd64" ? "linux-tar" : "linux-aarch64-tar" }'
  GO_ARCH: '#{ target.arch == "amd64" ? "x86_64" : "aarch64" }'
  JDK_DIGEST: sha256:99421ea725e0905500a94ed9b10b688071e6681002d06bd6869193f55111c366
  JDK_VERSION: 21.0.9.10
  SEMVER_MAJOR_MINOR_VERSION: "9.1"
  SEMVER_MAJOR_VERSION: "9"
  SEMVER_VERSION: 9.1.9
  SKIP_PATCHING: "FALSE"
  VERSION: 9.1.9
contents:
  packages:
    - '!libelogind0'
    - '!mawk'
    - '!original-awk'
    - base-files
    - bash
    - coreutils
    - findutils
    - grep
    - libstdc++6
    - tini
  builds:
    - name: elasticsearch
      uses: dhi.io/eclipse-temurin:21.0.9.10-jdk-debian13-dev@sha256:99421ea725e0905500a94ed9b10b688071e6681002d06bd6869193f55111c366
      privileged: true
      contents:
        packages:
          - bash
          - coreutils
          - findutils
          - sed
          - tini
        files:
          - url: git+https://github.com/elastic/elasticsearch.git#v9.1.9
            checksum: 5cf7cb8c6f69b8c284f3ea738db6932703574d3d
            path: ${source.dir}/elasticsearch
            spdx:
              name: elasticsearch
              version: 9.1.9
              packages:
                - name: elasticsearch
                  purl: pkg:dhi/elasticsearch@9.1.9
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: git patch elasticsearch
          uses: patch@v1
          work-dir: ${source.dir}/elasticsearch
          with:
            options: -p1
            patches:
              - ${source.dir}/patch/9.1.patch
              - ${source.dir}/patch/lz4.patch
        - name: build
          work-dir: ${source.dir}/elasticsearch
          runs: |
            set -eux -o pipefail

            ./gradlew localDistro
        - name: extract built tarball
          runs: |
            set -eux -o pipefail

            mkdir -p "${target.dir}/opt/elasticsearch/"
            cp -r "${source.dir}/elasticsearch/distribution/archives/${ELASTICSEARCH_OUTPUT_ARCHIVE_DIR}/build/install/elasticsearch-9.1.9-SNAPSHOT/." "${target.dir}/opt/elasticsearch/elasticsearch-9.1.9"
      paths:
        - type: file
          path: ${source.dir}/patch/9.1.patch
          uid: 0
          gid: 0
          source: image/elasticsearch/patch/9.1.patch
        - type: file
          path: ${source.dir}/patch/lz4.patch
          uid: 0
          gid: 0
          source: image/elasticsearch/patch/lz4-new.patch
      outputs:
        - source: ${target.dir}/opt/elasticsearch/elasticsearch-9.1.9
          target: /opt/elasticsearch/elasticsearch-9.1.9
          uid: 1000
          gid: 1000
        - source: /opt/docker/sbom/elasticsearch
          target: /opt/docker/sbom/elasticsearch
          uid: 0
          gid: 0
        - source: ${source.dir}/elasticsearch/distribution/docker/src/docker/bin/docker-entrypoint.sh
          target: /usr/local/bin/docker-entrypoint.sh
          uid: 1000
          gid: 1000
          mode: "0755"
accounts:
  run-as: elasticsearch
  users:
    - name: elasticsearch
      uid: 1000
      gid: 1000
  groups:
    - name: elasticsearch
      gid: 1000
      members:
        - elasticsearch
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
work-dir: /usr/share/elasticsearch
environment:
  ELASTIC_CONTAINER: "true"
  ELASTICSEARCH_VERSION: 9.1.9
  PATH: /opt/elasticsearch/elasticsearch-9.1.9/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
paths:
  - type: symlink
    path: /usr/share/elasticsearch
    uid: 0
    gid: 0
    source: /opt/elasticsearch/elasticsearch-9.1.9
  - type: file
    path: /opt/elasticsearch/elasticsearch-9.1.9/config/elasticsearch.yml
    uid: 0
    gid: 0
    mode: "0644"
    source: image/elasticsearch/config/elasticsearch.yml
  - type: file
    path: /opt/elasticsearch/elasticsearch-9.1.9/config/log4j2.properties
    uid: 0
    gid: 0
    mode: "0644"
    source: image/elasticsearch/config/log4j2.properties
annotations:
  org.opencontainers.image.description: A minimal Elasticsearch image
  org.opencontainers.image.licenses: Elastic-License-2.0
entrypoint:
  - /usr/bin/tini
  - --
  - /usr/local/bin/docker-entrypoint.sh
cmd:
  - eswrapper
ports:
  - 9200/tcp
  - 9300/tcp
