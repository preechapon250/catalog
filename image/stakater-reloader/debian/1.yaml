# syntax=dhi/build:1-debian13

# DO NOT EDIT THIS FILE MANUALLY
# generated with 'dhi def merge image/stakater-reloader/debian/1.yaml'

name: Stakater Reloader 1.x
image: dhi/stakater-reloader
variant: runtime
tags:
    - "1"
    - "1.4"
    - 1.4.12
    - v1.4.12
    - 1-debian13
    - 1.4-debian13
    - 1.4.12-debian13
platforms:
    - linux/amd64
    - linux/arm64
vars:
    ARCH: '#{ target.arch == "amd64" ? "amd64" : "arm64" }'
    COMMIT_SHA: 264b90df9d40a474308be258f4185996347de915
    GO_DIGEST: sha256:8c2284d70af13cb934da5f05f5fa58a776aa963e88ebf542970ba09f4e3a0bbf
    GO_VERSION: 1.25.5
    SEMVER_MAJOR_MINOR_VERSION: "1.4"
    SEMVER_MAJOR_VERSION: "1"
    SEMVER_VERSION: 1.4.12
    VERSION: 1.4.12
contents:
    packages:
        - '!dpkg'
        - '!gawk'
        - '!gcc-14'
        - '!gcc-14-base'
        - '!libelogind0'
        - '!libslang2'
        - '!mawk'
        - '!original-awk'
        - '!tar'
        - base-files
        - ca-certificates
    builds:
        - name: go
          uses: dhi/golang:1.25.5-debian13-dev@sha256:8c2284d70af13cb934da5f05f5fa58a776aa963e88ebf542970ba09f4e3a0bbf
          privileged: true
          contents:
            packages:
                - gzip
                - sed
            files:
                - url: git+https://github.com/stakater/Reloader.git#v1.4.12
                  path: ${source.dir}/Reloader
                  uid: 0
                  gid: 0
          pipeline:
            - name: verify
              uses: git/checkout@v1
              with:
                dir: Reloader
                expected-commit: 264b90df9d40a474308be258f4185996347de915
            - name: build
              runs: |
                set -eux -o pipefail

                mkdir -p "/go/src/github.com/stakater"
                ln -s "${source.dir}/Reloader" "/go/src/github.com/stakater"
                cd "/go/src/github.com/stakater/Reloader"

                # replace the build timestamp to a fixed value to make the build deterministic
                ts=$(git show -s --format=%cd --date=iso-strict HEAD)
                sed -i "s/DATE := .*/DATE := \"$ts\"/g" Makefile

                sed -i 's|LDFLAGS =.*|LDFLAGS = "-ldflags=-X github.com/stakater/Reloader/main.Version=1.4.12"|g' Makefile

                CGO_ENABLED=0 make build

                mkdir -p "${target.dir}/bin"
                cp Reloader "${target.dir}/bin"
          outputs:
            - source: ${target.dir}/bin
              target: /usr/local/bin
              uid: 0
              gid: 0
accounts:
    run-as: nonroot
    users:
        - name: nonroot
          uid: 65532
          gid: 65532
    groups:
        - name: nonroot
          gid: 65532
          members:
            - nonroot
os-release:
    name: Docker Hardened Images (Debian)
    id: debian
    version-id: "13"
    version-codename: trixie
    pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
    home-url: https://docker.com/products/hardened-images/
    bug-report-url: https://docker.com/support/
annotations:
    org.opencontainers.image.description: A minimal stakater/reloader image
    org.opencontainers.image.licenses: Apache-2.0
cmd:
    - Reloader
tests:
    - name: Run testcontainers tests
      directory: image/stakater-reloader/tests
      commands:
        - go test . -count=1 -v
      exit-code: 0
